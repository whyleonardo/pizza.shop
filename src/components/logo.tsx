import { Pizza } from "lucide-react"

export const Logo = () => {
	return (
		<div className="flex w-fit items-center gap-2 overflow-hidden p-1">
			<Pizza className="size-6 transition hover:scale-110" />
		</div>
	)
}
